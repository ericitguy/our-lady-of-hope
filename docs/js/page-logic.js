/* ============================================================
   OUR LADY OF HOPE ENTERPRISE — Page-specific logic
   (About Us: category grid; Our Story: timeline is in main.js)
   ============================================================ */
(function () {
  "use strict";
  const OLAH = window.OLAH;
  if (!OLAH) return;

  /* About page renders its own category grid (already handled by
     main.js via #categoryGrid). Nothing extra needed today —
     reserved for future page-specific behaviour. */
})();
